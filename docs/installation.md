# Installation

## Python package

```bash
pip install ipybox
```

## MCP server

`ipybox` can be installed as [MCP server](mcp-server.md).

```json
{
  "mcpServers": {
    "ipybox": {
      "command": "uvx",
      "args": ["ipybox", "mcp"]
    }
  }
}
```
